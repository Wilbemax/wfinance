import React from 'react'
import { Typography } from 'antd'

type Props = {}

const SheetHeader = (props: Props) => {
  return <Typography.Title level={4}>Добавь новый бюджет</Typography.Title>
}

export { SheetHeader }
